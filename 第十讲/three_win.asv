function [a,b,c]=three_win(x,y,n)
%x,y为白球黑球个数
m = zeros(x);  %0序号为白球
m = [m,ones(1)];  %1序号为黑球
number = x+y
a = 0;b = 0;c = 0;
for i = 1:n
   for j=1:7
       
    a_ = randi([1,x+y],1,7)
end
a/n  %A获胜概率
b/n  %B获胜概率
c/n  %C获胜概率